{
  "name": "client",
  "version": "1.0.0",
  "description": "TalkhubAPI Client",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^27.1.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/electron": "^1.6.10"
  }
}
